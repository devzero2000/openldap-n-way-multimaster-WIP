---
- name: "LDAP Cluster Deep Consistency Audit"
  hosts: all
  become: true
  serial: 0 # Esegue le attività di raccolta in parallelo su tutti i nodi
  vars:
    # --------------------------------------------------------------------------
    # CONFIGURAZIONE VARIABILI DI BASE
    # --------------------------------------------------------------------------
    root_dse: "dc=example,dc=com"
    ou_people: "ou=People,dc=example,dc=com"
    ou_groups: "ou=groups,dc=example,dc=com"
    
    # Percorsi di staging sui nodi remoti
    stage_dir: "/tmp/ldap_audit"
    
  tasks:
    - name: "[PREPARE] Crea la directory di staging locale su ogni nodo"
      ansible.builtin.file:
        path: "{{ stage_dir }}"
        state: directory
        mode: '0700'

    # ==========================================================================
    # FASE 1 & 2: ESTRAZIONE DATI VIA SLAPCAT (BYPASS SIZELIMIT)
    # ==========================================================================
    - name: "[DUMP] Estrae e normalizza i DN degli Utenti via slapcat"
      ansible.builtin.shell: >
        set -o pipefail &&
        slapcat -b "{{ root_dse }}" -a "(objectClass=inetOrgPerson)" 2>/dev/null | 
        grep "^dn: " | sort > "{{ stage_dir }}/users_dn.txt"
      changed_when: false
      args:
        executable: /bin/bash

    - name: "[DUMP] Estrae e normalizza i DN dei Gruppi via slapcat"
      ansible.builtin.shell: >
        set -o pipefail &&
        slapcat -b "{{ root_dse }}" -a "(objectClass=groupOfNames)" 2>/dev/null | 
        grep "^dn: " | sort > "{{ stage_dir }}/groups_dn.txt"
      changed_when: false
      args:
        executable: /bin/bash

    # ==========================================================================
    # FASE 3: CONTEGGIO STRUTTURATO SUI DN ESTRATTI
    # ==========================================================================
    - name: "[COUNT] Calcola il numero totale di utenti contando i DN"
      ansible.builtin.shell: >
        set -o pipefail &&
        grep -c "^dn: " "{{ stage_dir }}/users_dn.txt" || echo 0
      register: remote_user_count
      changed_when: false
      args:
        executable: /bin/bash

    - name: "[COUNT] Calcola il numero totale di gruppi contando i DN"
      ansible.builtin.shell: >
        set -o pipefail &&
        grep -c "^dn: " "{{ stage_dir }}/groups_dn.txt" || echo 0
      register: remote_group_count
      changed_when: false
      args:
        executable: /bin/bash

    # ==========================================================================
    # FASE 4: CENTRALIZZAZIONE DEI DATI SUL CONTROLLER
    # ==========================================================================
    - name: "[FETCH] Centralizza i report sul server di controllo Ansible"
      ansible.builtin.fetch:
        src: "{{ stage_dir }}/{{ item }}"
        dest: "/tmp/ldap_audit_master/{{ inventory_hostname }}/{{ item }}"
        flat: true
      loop:
        - "users_dn.txt"
        - "groups_dn.txt"

    - name: "[CLEANUP] Rimuove la directory di staging sui nodi remoti"
      ansible.builtin.file:
        path: "{{ stage_dir }}"
        state: absent

# ==============================================================================
# FASE 5: ANALISI DELLE DISCREPANZE (ELABORAZIONE LOCALE SUL CONTROLLER)
# ==============================================================================
- name: "Consolida ed Elabora le Discrepanze del Cluster"
  hosts: localhost
  gather_facts: false
  tasks:
    - name: "[ANALYSIS] Verifica l'omogeneità dei conteggi quantitativi"
      ansible.builtin.assert:
        that:
          - "hostvars[item]['remote_user_count']['stdout'] | trim == hostvars[groups['all'][0]]['remote_user_count']['stdout'] | trim"
          - "hostvars[item]['remote_group_count']['stdout'] | trim == hostvars[groups['all'][0]]['remote_group_count']['stdout'] | trim"
        fail_msg: >
          CRITICAL: Disallineamento quantitativo rilevato! 
          Il nodo {{ item }} ha {{ hostvars[item]['remote_user_count']['stdout'] | trim }} utenti e {{ hostvars[item]['remote_group_count']['stdout'] | trim }} gruppi.
          Il nodo di riferimento {{ groups['all'][0] }} ha {{ hostvars[groups['all'][0]]['remote_user_count']['stdout'] | trim }} utenti e {{ hostvars[groups['all'][0]]['remote_group_count']['stdout'] | trim }} gruppi.
        success_msg: "Allineamento quantitativo OK per il nodo {{ item }}."
      loop: "{{ groups['all'] }}"

    - name: "[ANALYSIS] Genera i file di Diff incrociati per gli Utenti"
      ansible.builtin.shell: >
        set -o pipefail &&
        diff -u /tmp/ldap_audit_master/{{ groups['all'][0] }}/users_dn.txt /tmp/ldap_audit_master/{{ item }}/users_dn.txt || true
      register: users_diff_output
      loop: "{{ groups['all'] }}"
      changed_when: false
      args:
        executable: /bin/bash

    - name: "[ANALYSIS] Genera i file di Diff incrociati per i Gruppi"
      ansible.builtin.shell: >
        set -o pipefail &&
        diff -u /tmp/ldap_audit_master/{{ groups['all'][0] }}/groups_dn.txt /tmp/ldap_audit_master/{{ item }}/groups_dn.txt || true
      register: groups_diff_output
      loop: "{{ groups['all'] }}"
      changed_when: false
      args:
        executable: /bin/bash

    - name: "[REPORT] Mostra in modo chiaro le entry orfane o mancanti e i totali per nodo"
      ansible.builtin.debug:
        msg: >
          {% set index = ansible_loop.index0 %}
          === STATISTICHE NODO TARGET: [{{ item }}] ===
          Totale Utenti validi (corrispondenza DN): {{ hostvars[item]['remote_user_count']['stdout'] | trim }}
          Totale Gruppi validi (corrispondenza DN): {{ hostvars[item]['remote_group_count']['stdout'] | trim }}
          
          === CONFRONTO NODI: RIFERIMENTO [{{ groups['all'][0] }}] vs TARGET [{{ item }}] ===
          
          --- DISCREPANZE UTENTI ---
          {{ users_diff_output.results[index].stdout | default('Nessuna discrepanza (100% allineati)') }}
          
          --- DISCREPANZE GRUPPI ---
          {{ groups_diff_output.results[index].stdout | default('Nessuna discrepanza (100% allineati)') }}
      loop: "{{ groups['all'] }}"
      loop_control:
        extended: true
